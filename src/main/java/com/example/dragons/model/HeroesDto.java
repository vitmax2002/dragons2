package com.example.dragons.model;

public record HeroesDto(Integer id,
                        Integer hp,
                        String name,
                        String clasa,
                        Integer roomId) {
}
